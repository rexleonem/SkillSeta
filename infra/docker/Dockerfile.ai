FROM node:22-alpine AS base
WORKDIR /app
COPY . .
RUN corepack enable && pnpm install --frozen-lockfile=false
RUN pnpm --filter @skillseta/ai-service build
CMD ["pnpm", "--filter", "@skillseta/ai-service", "start"]
