FROM node:22-alpine AS base
WORKDIR /app
COPY . .
RUN corepack enable && pnpm install --frozen-lockfile=false
RUN pnpm --filter @skillseta/admin build
CMD ["pnpm", "--filter", "@skillseta/admin", "preview", "--host", "0.0.0.0", "--port", "3001"]
